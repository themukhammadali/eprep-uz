/*
 * Supabase configuration for eprep.uz.
 *
 * Fill in the two values below with your project's credentials, then reload.
 * Find them in the Supabase dashboard: Project Settings -> API.
 *
 *   url     -> "Project URL"        (e.g. https://abcdefgh.supabase.co)
 *   anonKey -> "anon / public" key  (safe to expose in a static site; access
 *              is restricted by Row-Level Security — see scripts/supabase-schema.sql)
 *
 * Leave them empty to run the app exactly as before (local-only, no accounts).
 */
window.SUPABASE_CONFIG = {
  url: "",
  anonKey: "",
};
